# 2026-06-17

## U7-155H Ping Poller (07:17 UTC)
- Cron job fired; fixed bug: `sender` was undefined in `check_session()` — referenced instead of calling `extract_sender(content)`
- Ping detected in **#sandbox-unit8**: *"@Ebi Shrimp @U7-155H im the one behind CPU u think i give up so easily"* — response attempted via `openclaw agent` delivery
- Script ran cleanly post-fix
# 2026-06-17 — Daily Notes

## Time recorded: 07:49 UTC

### Summary
Commander wants the Discord bot (JarvisV15.0) to have todo functionality stored in Supabase (external database).

### Key events
- **Site 502 fixed**: Python site server (port 8002) had crashed. Restarted via tmux. Chat API (Flask port 8003) was fine.
- **Chat widget → real AI**: Replaced canned Flask responses with actual OpenClaw CLI calls (`openclaw agent --message`). Chat now talks to real Jarvis via the gateway. Conversational sessions maintained server-side in Python dict (session_key → history).
- **Discord bot**: Currently configured with DeepSeek V4 Flash model, `allowFrom: ["*"]`, no mention required. In 2 servers.
- **Model**: `openrouter/deepseek/deepseek-v4-flash` — reverted from Gemini 3.5 Flash due to cost (OpenRouter credit limit).
- **Memory search**: Disabled (`enabled: false`) to avoid "Missing API key for openai" error.

### New request
- **Todo functionality for Discord bot (JarvisV15.0)**
- **Data stored in Supabase** (external database)
- Arch details TBD — need to check if Supabase project exists, or create one

### Todo System — Complete! (08:10 UTC)
- **Supabase project**: created at `faamhecvascgwpqkubxy.supabase.co`
- **Database**: `todos` table with columns: id (auto), user_id (text), task (text), done (bool), created_at (timestamptz)
- **CLI script**: `scripts/todo.py` — add/list/done/remove/pending commands
- **Service role key**: [REDACTED — stored in todo.py]
- **Integration**: todo instructions added to TOOLS.md so Jarvis sees them on each turn
- **Per-user**: todos scoped by Discord user ID; Jarvis detects user from message context
- **Natural language**: Jarvis handles "add X to my todos" / "show my todos" / "mark todo N done" etc.

### Notes
- Chat widget uses real AI via OpenClaw CLI (Flask on 8003)
- Site on port 8002 via Python http.server
- Both proxied through nginx at `/qwertzine/` and `/api/chat`
- Discord bot (JarvisV15.0) is live with DeepSeek V4 Flash
